[package]
name = "arbitrary-fuzz"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
arbitrary = { version = "1", features = ["derive"] }
ziggy = { path = "../../", default-features = false }
